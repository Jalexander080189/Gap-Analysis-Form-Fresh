
import { redirect } from 'next/navigation';

export default function HomePage() {
  redirect('/reports/new-analysis');
  // This component will not render anything as redirect will happen server-side or client-side before render.
  // return null; // Or some loading/placeholder if needed, but redirect should be immediate.
}

